# Session log - 2026-07-28

## Shipped
- /report forensic tier at 0.03 USDC, catalogued in the Bazaar with its own schema
- Three-state signal envelope: known / unknown / not_applicable, all three exercised in production
- confidence and structured reasons (severity, signal, detail, contribution) exposed in paid responses
- _tokenData stripped from all external responses
- Oracle 0.4.2: raw signals block, age from earliest observed pair, pool provenance, observed cross-pool totals
- Megacap sentinels (999999999 liquidity, 99999 hours) replaced with null plus MEGACAP_MODE note
- Cached and fresh oracle responses now return the same shape
- holder_concentration states measurement basis and limitations

## Bugs found and fixed
- /report signed an undefined risk_score because signReceiptV2 read a flat shape while /report
  passed a nested one. canonical() rendered undefined, JSON.stringify dropped it, verification
  failed. Signer now refuses to sign missing fields; canonical treats undefined as null.
- Pool provenance was set in the DexScreener helper but never copied into tokenData by
  fetchTokenData's explicit field list.
- Token age came from the deepest pool's creation date. A sub-1% liquidity difference between
  two nearly-tied BONK pools swung reported age by 19,000 hours. Now uses the earliest pair.

## Corrected before filing
The Bazaar "write-once" finding was wrong. The entry did refresh - after roughly 15 hours,
not the 2.4 hours originally observed. Issue draft rewritten as refresh latency.

## Parked decisions (owner: Seun)
1. Publish scoring weights in an impact block per signal? The oracle gates weights behind a
   Pro tier and debug flag; publishing them in a 0.03 report contradicts that.
2. Filter program-owned accounts from holder concentration? JUP reads 91.42% across 20 token
   accounts, which for an established token means pools and custody, not whale ownership.
   Fixing it changes scores across every mature token and needs its own version bump.

## Still to do
- File the three upstream issue drafts (docs/upstream/) - drafted, not filed
- Send the x402 team outreach after the issues are visible
- Receipt schema convergence between Gate (execution_receipt/1.0) and x402 (survivor.receipt.v2)

## Verification evidence
JUP /report: 59/HIGH, all seven signals populated with provenance, receipt valid.
Settlement 3mDvGvdQbgt8u1tDNkcLPEztMQ5if2pXnkZo9RdFSssvKHSRGXannXzuciiU8ucYUEasDDJDUEeprCELrRSD2vei
wSOL megacap: score 85 held, all signals not_applicable, no sentinels leaked.
